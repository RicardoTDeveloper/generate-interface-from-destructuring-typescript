{
  "name": "generate-interface-from-destructuring--typescript-",
	"displayName": "Generate Interface from destructuring (Typescript)",
	"description": "This extension was developed to meet a personal demand, which basically converts a javascript destructuring to a typescript interface.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.69.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
        "onCommand:generate-interface-from-destructuring--typescript-.helloWorld"
	],
	"main": "./extension.js",
	"contributes": {
		"commands": [{
            "command": "generate-interface-from-destructuring--typescript-.helloWorld",
            "title": "Hello World"

		}]
	},
	"scripts": {
		"lint": "eslint .",
		"pretest": "yarn run lint",
		"test": "node ./test/runTest.js"
	},
	"devDependencies": {
        "@types/vscode": "^1.69.0",
        "@types/glob": "^7.2.0",
        "@types/mocha": "^9.1.1",
        "@types/node": "16.x",
        "eslint": "^8.18.0",
        "glob": "^8.0.3",
        "mocha": "^10.0.0",
        "typescript": "^4.7.4",
        "@vscode/test-electron": "^2.1.5"
    }

}
